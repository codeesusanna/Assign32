n=int(input())
l=[]
t=[]
for i in range(n):
    x=int(input())
    l.append(x)
print('given list=',l)
c=0        
for i in range(n):
    if l[i]!=0:
        t.append(l[i])
        c+=1
print('non-zero elements=',t)
print('count of non-zero elements=',c)
